<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/rootLayout"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:padding="16dp"
    android:gravity="center_horizontal">

    <!-- Top bar with back button + title -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:gravity="center_vertical">

        <ImageButton
            android:id="@+id/btnBack"
            android:layout_width="40dp"
            android:layout_height="40dp"
            android:background="?attr/selectableItemBackgroundBorderless"
            android:src="@drawable/ic_back"
            android:contentDescription="Back"
            android:tint="@color/black" />

        <TextView
            android:id="@+id/tvStatus"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginStart="12dp"
            android:text="Waiting..."
            android:textColor="@color/black"
            android:textSize="20sp"
            android:textStyle="bold" />
    </LinearLayout>

    <GridLayout
        android:id="@+id/gridBoard"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1"
        android:layout_marginTop="24dp"
        android:columnCount="3"
        android:rowCount="3">

        <ImageButton android:id="@+id/cell0" style="@style/BoardCell"/>
        <ImageButton android:id="@+id/cell1" style="@style/BoardCell"/>
        <ImageButton android:id="@+id/cell2" style="@style/BoardCell"/>
        <ImageButton android:id="@+id/cell3" style="@style/BoardCell"/>
        <ImageButton android:id="@+id/cell4" style="@style/BoardCell"/>
        <ImageButton android:id="@+id/cell5" style="@style/BoardCell"/>
        <ImageButton android:id="@+id/cell6" style="@style/BoardCell"/>
        <ImageButton android:id="@+id/cell7" style="@style/BoardCell"/>
        <ImageButton android:id="@+id/cell8" style="@style/BoardCell"/>
    </GridLayout>

</LinearLayout>
